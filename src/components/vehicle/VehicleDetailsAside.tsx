/*import React from "react";
import { VehicleData } from "../layers/VehicleLayerComponent";

interface IVehicleDetails {
  selectedVehicle: VehicleData; // Endret til VehicleData
}

const VehicleDetailsAside: React.FC<IVehicleDetails> = ({
  selectedVehicle,
}) => {
  console.log("Selected vehicle:", selectedVehicle);
  return (
    <aside className="bg-secondary rounded-3 mt-3 text-center text-white p-1">
      <h2>Kjøretøydetaljer</h2>
      <hr />
      <p>
        <b>Linje:</b> {selectedVehicle.line.publicCode}
      </p>
      <p>
        <b>Navn:</b> {selectedVehicle.line.lineName}
      </p>
      <p>
        <b>Sist oppdatert:</b> {selectedVehicle.lastUpdated}
      </p>
    </aside>
  );
};

export default VehicleDetailsAside;
*/
